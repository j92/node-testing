const DB = function(args) {
    this.getMissionByLaunchDate = function (missionDate, next) {

    };

    this.createNextMission = function (args, next) {

    };

    this.saveAssignment = function (args, next) {
    }
};

module.exports = DB;
